import React from "react";
import { PropertyImagesProps } from "@/app/types";

const PropertyImages = ({ images }: PropertyImagesProps) => {
  return <div>PropertyImages</div>;
};

export default PropertyImages;
