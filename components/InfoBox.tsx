import { InfoBoxProps } from "@/app/types";
import React from "react";

const InfoBox = ({
  heading,
  backgroundColor,
  textColor,
  buttonInfo,
  children,
}: InfoBoxProps) => {
  return <div></div>;
};

export default InfoBox;
